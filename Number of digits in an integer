#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n, rem, count=0;
    cout<<"enter a number" <<endl;
    cin>>n;
   while(n!=0)
   {
    n = n / 10;
    count ++;
   }
    cout<<"number of digit is:" << " " <<count<<endl;
    return 0;
}

enter a number
123
number of digit is: 3
