#include<bits/stdc++.h>
using namespace std;
int main()
{
    char str[60];
    cout<<"enter a string" <<endl;
    cin>>str;
    int length = 0;
    length = strlen(str);
    for(int i = length; i >= 0; i--)
    {
        cout<< str[i];
    }
    return 0;
}

enter a string
aakriti
itirkaa
